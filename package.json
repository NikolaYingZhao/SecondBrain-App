{
  "name": "secondbrain",
  "version": "1.0.0",
  "description": "A private, local-first desktop home for a personal knowledge base.",
  "author": "Nikola Ying Zhao",
  "private": true,
  "type": "module",
  "main": "src/desktop.mjs",
  "scripts": {
    "audit": "node src/cli.mjs audit",
    "build": "node src/cli.mjs build",
    "start": "electron .",
    "web": "node src/server.mjs",
    "test": "node --test && node scripts/guard-data-branch.mjs",
    "pack": "npm test && electron-builder --win dir",
    "dist": "npm test && electron-builder --win nsis"
  },
  "dependencies": {
    "marked": "^18.0.6",
    "sanitize-html": "^2.17.0"
  },
  "devDependencies": {
    "electron": "^37.2.3",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.secondbrain.desktop",
    "productName": "SecondBrain",
    "asar": true,
    "electronDist": "node_modules/electron/dist",
    "artifactName": "SecondBrain-Setup-${version}.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "config/**/*",
      "public/**/*",
      "src/**/*",
      "package.json"
    ],
    "win": {
      "icon": "build/icon.png",
      "requestedExecutionLevel": "asInvoker",
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "SecondBrain",
      "runAfterFinish": true,
      "deleteAppDataOnUninstall": false
    }
  }
}
